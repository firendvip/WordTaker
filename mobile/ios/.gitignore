# Xcode build artifacts
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# macOS
.DS_Store

# The .xcodeproj is generated by `xcodegen generate` from project.yml.
# It is kept in the repo so the project is turnkey to open. If you prefer to
# treat it as a build artifact, uncomment the next line and regenerate locally.
# WordTaker.xcodeproj/
